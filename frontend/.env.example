NEXT_PUBLIC_API_URL=http://localhost:4100
# Optionnel : sans cette clé, les champs "Ville" restent de simples champs
# texte avec une liste de suggestions limitée aux grandes villes. Active les
# API "Places API" et "Maps JavaScript API" sur https://console.cloud.google.com
# puis restreins la clé au domaine du site avant de la renseigner ici.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
