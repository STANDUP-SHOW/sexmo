// Shared across every useJsApiLoader({ id: 'sexmo-google-maps' }) call site —
// the loader errors if the same id is initialized with different `libraries`.
export const GOOGLE_MAPS_LIBRARIES = ['places'];
